package com.redteam.vulndb.entity;

/**
 * Enum defining the severity levels of a vulnerability,
 * loosely based on CVSS scoring categories.
 */
public enum Severity {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
